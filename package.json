{
  "name": "logmaster",
  "version": "1.0.0",
  "description": "LogMaster - Sistema de Control de Acceso de Visitantes",
  "scripts": {
    "dev": "cross-env NODE_OPTIONS=--no-warnings concurrently \"npm run server\" \"npm run client\"",
    "start": "cross-env NODE_OPTIONS=--no-warnings concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd client && npm install",
    "test:server": "npm --prefix server run test",
    "test:client": "npm --prefix client run test",
    "test": "npm run test:server && npm run test:client",
    "build:server": "npm --prefix server install && npx tsc -p server/tsconfig.json",
    "build:client": "npm --prefix client run build",
    "build": "npm run build:client && npm run build:server",
    "seed:clean": "cd server && npm run seed:clean",
    "backup": "npm --prefix server run backup:full",
    "backup:monitor": "npm --prefix server run backup:monitor"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "concurrently": "^8.2.2",
    "cross-env": "^10.1.0",
    "typescript": "^5.9.3"
  },
  "author": "",
  "license": "ISC"
}
